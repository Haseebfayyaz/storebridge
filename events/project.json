{
  "name": "events",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "events/src",
  "projectType": "library",
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nx/js:swc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/events",
        "main": "events/src/index.ts",
        "tsConfig": "events/tsconfig.lib.json",
        "assets": ["events/*.md"]
      }
    }
  }
}
